import * as attendanceRepo from '../repositories/attendance.repository';

export const createAttendance = attendanceRepo.createAttendance;
export const getAttendanceById = attendanceRepo.getAttendanceById;
export const updateAttendance = attendanceRepo.updateAttendance;
export const deleteAttendance = attendanceRepo.deleteAttendance;
